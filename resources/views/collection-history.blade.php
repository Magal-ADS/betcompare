<!DOCTYPE html>
<html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>OddRadar — Histórico</title><link rel="icon" type="image/svg+xml" href="{{ asset('favicon.svg') }}"><link rel="alternate icon" href="{{ asset('favicon.ico') }}">@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="min-h-screen bg-slate-950 text-slate-100"><main class="mx-auto max-w-6xl px-4 py-8 sm:px-6 lg:px-8">
<header class="flex items-center justify-between border-b border-slate-800 pb-6"><div><p class="text-sm font-semibold tracking-[.2em] text-cyan-400">ODDRADAR</p><h1 class="mt-1 text-3xl font-semibold text-white">Histórico de coletas</h1></div><a class="rounded-lg border border-slate-700 px-4 py-2 text-sm font-semibold text-slate-200 hover:bg-slate-800" href="{{ route('dashboard') }}">Voltar ao painel</a></header>
<div class="mt-8 overflow-x-auto rounded-xl border border-slate-800 bg-slate-900"><table class="min-w-full text-left text-sm"><thead class="bg-slate-950/50 text-xs uppercase text-slate-400"><tr><th class="px-5 py-3">Atualização</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Fontes</th></tr></thead><tbody class="divide-y divide-slate-800">@forelse($collectionRuns as $run)<tr><td class="px-5 py-4">{{ $run->finished_at?->setTimezone(config('app.display_timezone'))->format('d/m/Y H:i') ?? 'Em processamento' }}</td><td class="px-4 py-4">{{ $run->status }}</td><td class="px-4 py-4">@foreach($run->sourceResults as $result)<span class="mr-2 inline-block">{{ $result->bookmaker->name }}: {{ $result->status === 'completed' ? $result->events_count.' eventos' : 'falhou' }}</span>@endforeach</td></tr>@empty<tr><td class="px-5 py-8 text-slate-400" colspan="3">Nenhuma coleta registrada.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $collectionRuns->links() }}</div>
</main></body></html>
